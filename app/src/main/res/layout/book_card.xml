<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:card_view="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  card_view:cardCornerRadius="8dp"
  card_view:contentPaddingTop="10dp"
  card_view:layout_goneMarginTop="10dp"
  card_view:cardUseCompatPadding="true">

  <!--<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"

    android:background="?android:selectableItemBackground">
    <TextView
      style="@style/RecycleView.Id"
      android:textSize="16sp"

      android:id="@+id/tvBookdId"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:gravity="end"
      android:text="@string/default_id"/>
    <TextView
      android:id="@+id/tvBookName"
      style="@style/RecycleView.Items"
      android:layout_below="@id/tvBookdId"
      android:text="@string/default_name"/>
    <TextView
      android:id="@+id/tvBookAuthor"
      style="@style/RecycleView.Items"
      android:layout_below="@id/tvBookName"
      android:text="@string/default_book_author"/>
    <TextView
      android:id="@+id/tvBookCompany"
      style="@style/RecycleView.Items"
      android:layout_below="@+id/tvBookAuthor"
      android:text="@string/default_book_company"/>
    <TextView
      android:id="@+id/tvBookYear"
      style="@style/RecycleView.Items"
      android:layout_below="@id/tvBookCompany"
      android:text="@string/default_book_years"/>

  </RelativeLayout>-->

</android.support.v7.widget.CardView>